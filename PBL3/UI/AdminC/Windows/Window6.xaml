<UserControl x:Class="PBL3.UI.AdminC.Windows.HoSoGiaoVien"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F5F6FA">

    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5,8"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="BorderBrush" Value="#DCDDE1"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style TargetType="Button" x:Key="BtnPrimary">
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="BtnDanger" BasedOn="{StaticResource BtnPrimary}">
            <Setter Property="Background" Value="#E74C3C"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Margin="15">
            <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="DANH SÁCH GIÁO VIÊN" FontSize="22" FontWeight="Bold" Foreground="#1976D2" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBox x:Name="txtSearch" Width="200" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                    <Button Content="Tìm kiếm" Style="{StaticResource BtnPrimary}" Click="BtnSearch_Click" Margin="0"/>
                </StackPanel>
            </Grid>

            <Border Background="White" CornerRadius="8" Padding="5">
                <DataGrid x:Name="dgGiaoVien" AutoGenerateColumns="False" IsReadOnly="True" 
                          SelectionMode="Single" SelectionChanged="DgGiaoVien_SelectionChanged"
                          RowBackground="White" AlternatingRowBackground="#F9F9F9"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã GV" Binding="{Binding MaGV}" Width="80"/>
                        <DataGridTextColumn Header="Họ Tên" Binding="{Binding HoTen}" Width="*"/>
                        <DataGridTextColumn Header="Chuyên Môn" Binding="{Binding ChuyenMon}" Width="120"/>
                        <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding SDT}" Width="100"/>
                        <DataGridTextColumn Header="Tình Trạng" Binding="{Binding TinhTrang}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </DockPanel>

        <Border Grid.Column="1" Background="White" CornerRadius="8" Margin="0,15,15,15" Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="HỒ SƠ GIÁO VIÊN" FontSize="18" FontWeight="Bold" Foreground="#333" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                    <Label Content="Mã giáo viên (*)"/>
                    <TextBox x:Name="txtMaGV" />

                    <Label Content="Họ và tên (*)"/>
                    <TextBox x:Name="txtHoTen" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <Label Content="Ngày sinh"/>
                            <DatePicker x:Name="dpNgaySinh" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                            <Label Content="Giới tính"/>
                            <ComboBox x:Name="cmbGioiTinh">
                                <ComboBoxItem Content="Nam"/>
                                <ComboBoxItem Content="Nữ"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <Label Content="Số điện thoại (*)"/>
                    <TextBox x:Name="txtSDT" />

                    <Label Content="Địa chỉ Email"/>
                    <TextBox x:Name="txtEmail" />

                    <Label Content="Chuyên môn (*)"/>
                    <ComboBox x:Name="cmbChuyenMon">
                        <ComboBoxItem Content="Toán học"/>
                        <ComboBoxItem Content="Ngữ văn"/>
                        <ComboBoxItem Content="Vật lý"/>
                        <ComboBoxItem Content="Ngoại ngữ"/>
                        <ComboBoxItem Content="Lịch sử"/>
                    </ComboBox>

                    <Label Content="Tình trạng"/>
                    <ComboBox x:Name="cmbTinhTrang">
                        <ComboBoxItem Content="Đang dạy"/>
                        <ComboBoxItem Content="Nghỉ việc/Hưu"/>
                    </ComboBox>

                    <WrapPanel HorizontalAlignment="Center" Margin="0,20,0,0">
                        <Button Content="Thêm mới" Style="{StaticResource BtnPrimary}" Click="BtnAdd_Click"/>
                        <Button Content="Cập nhật" Style="{StaticResource BtnPrimary}" Click="BtnEdit_Click"/>
                        <Button Content="Xóa" Style="{StaticResource BtnDanger}" Click="BtnDelete_Click"/>
                        <Button Content="Làm mới" Style="{StaticResource BtnPrimary}" Background="#7F8C8D" Click="BtnRefresh_Click"/>
                    </WrapPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>