<UserControl x:Class="PBL3.UI.AdminC.Windows.HoSoGiaoVien"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xml:lang="vi-VN"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F4F8FB">

    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#90CAF9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#90CAF9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#90CAF9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style TargetType="Button" x:Key="BtnPrimary">
            <Setter Property="Background" Value="#1E88E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1565C0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource BtnPrimary}" x:Key="BtnDanger">
            <Setter Property="Background" Value="#EF5350"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C62828"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource BtnPrimary}" x:Key="BtnSecondary">
            <Setter Property="Background" Value="#78909C"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#546E7A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="36"/>
            <Setter Property="ColumnHeaderHeight" Value="38"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E3EEF7"/>
            <Setter Property="VerticalGridLinesBrush" Value="#E3EEF7"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D0E3F1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9FCFF"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#E3F2FD"/>
            <Setter Property="Foreground" Value="#0D47A1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8 0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="CenterAlignStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LeftAlignStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="15,0,0,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="18">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="575*"/>
            <ColumnDefinition Width="489*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" 
                Background="White" 
                CornerRadius="14" 
                Padding="14" 
                BorderBrush="#D6E6F2" 
                BorderThickness="1" 
                Margin="0,0,10,0">

            <DockPanel>
                <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Danh sách giáo viên" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="#0D47A1" 
                               VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">

                        <ComboBox x:Name="cbbChuyenMon" Width="110" Margin="0,0,8,0" SelectionChanged="cbbChuyenMon_SelectionChanged">
                            <ComboBoxItem Content="Tất cả" IsSelected="True"/>
                            <ComboBoxItem Content="Toán"/>
                            <ComboBoxItem Content="Lý"/>
                            <ComboBoxItem Content="Hóa"/>
                            <ComboBoxItem Content="Sinh"/>
                            <ComboBoxItem Content="Văn"/>
                            <ComboBoxItem Content="Tiếng Anh"/>
                        </ComboBox>

                        <Grid Width="220" Margin="0,0,8,0">
                            <TextBox x:Name="txtSearch" Margin="0"/>
                            <TextBlock IsHitTestVisible="False" 
                                       Text="Nhập Mã GV hoặc Tên..." 
                                       VerticalAlignment="Center" 
                                       HorizontalAlignment="Left" 
                                       Margin="10,0,0,0" 
                                       Foreground="#A0A0A0"
                                       FontStyle="Italic">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=txtSearch}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>

                        <Button Content="Tìm kiếm" Style="{StaticResource BtnPrimary}" Click="BtnSearch_Click" Margin="0" Width="90"/>
                    </StackPanel>
                </Grid>

                <DataGrid x:Name="dgvGiaoVien" 
                          IsReadOnly="True" 
                          SelectionChanged="dgvGiaoVien_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã GV" Binding="{Binding MaGV}" Width="90" ElementStyle="{StaticResource CenterAlignStyle}"/>
                        <DataGridTextColumn Header="Họ Tên" Binding="{Binding HoTen}" Width="180" ElementStyle="{StaticResource LeftAlignStyle}"/>
                        <DataGridTextColumn Header="Chuyên Môn" Binding="{Binding ChuyenMon}" Width="120" ElementStyle="{StaticResource CenterAlignStyle}"/>
                        <DataGridTextColumn Header="SĐT" Binding="{Binding SDT}" Width="100" ElementStyle="{StaticResource CenterAlignStyle}"/>
                        <DataGridTextColumn Header="Tình Trạng" Binding="{Binding TinhTrang}" Width="100" ElementStyle="{StaticResource CenterAlignStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <Border Grid.Column="1"
                Background="White" 
                CornerRadius="14" 
                Padding="16" 
                BorderBrush="#D6E6F2" 
                BorderThickness="1" 
                Margin="1,0,0,0">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Thông Tin Hồ Sơ" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="#0D47A1" 
                               Margin="0,0,0,15" 
                               HorizontalAlignment="Center"/>

                    <Label Content="Mã giáo viên (*)"/>
                    <TextBox x:Name="txtMaGV" IsReadOnly="True" />

                    <Label Content="Họ và tên (*)"/>
                    <TextBox x:Name="txtHoTen" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,6,0">
                            <Label Content="Ngày sinh"/>
                            <DatePicker x:Name="dpNgaySinh" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="6,0,0,0">
                            <Label Content="Giới tính"/>
                            <ComboBox x:Name="cmbGioiTinh">
                                <ComboBoxItem Content="Male"/>
                                <ComboBoxItem Content="Female"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <Label Content="Số điện thoại (*)"/>
                    <TextBox x:Name="txtSDT" />

                    <Label Content="Chuyên môn (*)"/>
                    <TextBox x:Name="txtChuyenMon" IsReadOnly="True" Background="#F9FCFF"/>

                    <WrapPanel HorizontalAlignment="Center" Margin="0,15,0,0">
                        <Button Content="Cập nhật" Style="{StaticResource BtnPrimary}" Width="95" Click="BtnEdit_Click"/>
                        <Button Content="Làm mới" Style="{StaticResource BtnSecondary}" Width="95" Click="BtnRefresh_Click"/>
                    </WrapPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>