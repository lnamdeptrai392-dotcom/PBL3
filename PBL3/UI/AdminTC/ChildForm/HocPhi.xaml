<UserControl x:Class="PBL3a.UI.AdminTC.HocPhi"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White"
             Width="800"
             Height="500">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Khối:" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
            <ComboBox x:Name="cbbGrade" Width="100" Height="35" Margin="0,0,30,0" 
                      SelectionChanged="Filter_SelectionChanged">
                <ComboBoxItem Content="10"/>
                <ComboBoxItem Content="11"/>
                <ComboBoxItem Content="12"/>
            </ComboBox>

            <TextBlock Text="Môn học:" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
            <ComboBox x:Name="cbbCourse" Width="150" Height="35" Margin="0,0,30,0"
                      SelectionChanged="Filter_SelectionChanged">
                <ComboBoxItem Content="Toán" Tag="MAT"/>
                <ComboBoxItem Content="Văn" Tag="LIT"/>
                <ComboBoxItem Content="Anh" Tag="ENG"/>
                <ComboBoxItem Content="Lý" Tag="PHY"/>
                <ComboBoxItem Content="Hóa" Tag="CHE"/>
                <ComboBoxItem Content="Sinh" Tag="BIO"/>
            </ComboBox>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Mã Lớp:" FontSize="16" VerticalAlignment="Center"/>
            <ComboBox x:Name="cbbML" Grid.Column="1" Height="35" 
                      SelectionChanged="cbbML_SelectionChanged"/>

            <TextBlock Text="Tên Lớp:" Grid.Column="2" FontSize="16" VerticalAlignment="Center" Margin="15,0,0,0"/>
            <TextBox x:Name="tbTL" Grid.Column="3" Height="35" IsReadOnly="True" VerticalAlignment="Center"/>
        </Grid>
        
        <DataGrid x:Name="dataGridView1"
                  Grid.Row="2"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Background="White"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  Margin="0,10,0,10"
                  BorderBrush="#5D9CB3" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã HS" Binding="{Binding AccountID}" Width="100"/>
                <DataGridTextColumn Header="Họ Tên" Binding="{Binding HoTen}" Width="*"/>
                <DataGridTextColumn Header="Tháng" Binding="{Binding TuitionMonth}" Width="60"/>
                <DataGridTextColumn Header="Số tiền" Binding="{Binding SoTien, StringFormat={}{0:N0} VNĐ}" Width="120"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btSetHP"
                    Content="Thiết lập học phí"
                    Width="180"
                    Height="45"
                    Background="#5D9CB3"
                    Foreground="White"
                    FontSize="15"
                    BorderThickness="0"
                    Click="btSetHP_Click"/>
        </StackPanel>
    </Grid>
</UserControl>