<UserControl x:Class="PBL3.UI.AdminTC.ChildForm.TongQuan"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
mc:Ignorable="d"
Background="#F0F2F5" d:DesignWidth="800"  d:DesignHeight="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Rectangle Width="5" Height="30" Fill="#1E88E5" Margin="0,0,10,0"/>
            <TextBlock Text="TỔNG QUAN" FontSize="22" FontWeight="Bold" Foreground="#1565C0" VerticalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" Margin="0,0,488,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="7*"/>

                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Chọn danh sách" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#1E88E5"/>
                <ComboBox Grid.Row="1" x:Name="cbbList" Width="232" Height="32" Margin="0,0,0,0" SelectionChanged="cbbList_SelectionChanged">
                    <ComboBoxItem Content="Học sinh chưa nộp học phí"/>
                    <ComboBoxItem Content="Giáo viên chưa thanh toán lương"/>
                    <ComboBoxItem Content="Khoản chi/thu cần thanh toán khác"/>
                </ComboBox>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Danh sách" FontWeight="SemiBold" Margin="5,0,0,10" Foreground="#1565C0"/>
                <DataGrid x:Name="dgvDanhSach" Grid.Row="1" AutoGenerateColumns="True" IsReadOnly="True"
                      Background="White" BorderThickness="1" BorderBrush="#E0E0E0" GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F0F0F0" RowHeight="35">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#E3F2FD"/>
                            <Setter Property="Foreground" Value="#1565C0"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,5"/>
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#BBDEFB"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
