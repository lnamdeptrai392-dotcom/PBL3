<UserControl x:Class="PBL3a.UI.Student.StudentINFO"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="#F0F2F5" 
             d:DesignWidth="800" d:DesignHeight="600">

    <Grid Margin="30">
        <Border Background="White" CornerRadius="15" MaxWidth="600" VerticalAlignment="Top" Padding="30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="THÔNG TIN CÁ NHÂN" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#1565C0" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,30"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#555"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="0,0,0,20"/>
                        </Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Height" Value="35"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="Margin" Value="0,0,0,20"/>
                        </Style>
                        <Style TargetType="DatePicker">
                            <Setter Property="Height" Value="35"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="0,0,0,20"/>
                        </Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="Height" Value="35"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="0,0,0,20"/>
                        </Style>
                    </Grid.Resources>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Họ tên:"/>
                    <TextBox x:Name="txtHoTen" Grid.Row="0" Grid.Column="1" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Ngày sinh:"/>
                    <DatePicker x:Name="dtpNgaySinh" Grid.Row="1" Grid.Column="1" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Giới tính:"/>
                    <ComboBox x:Name="cboGioiTinh" Grid.Row="2" Grid.Column="1">
                        <ComboBoxItem Content="Nam"/>
                        <ComboBoxItem Content="Nữ"/>
                    </ComboBox>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Email:"/>
                    <TextBox x:Name="txtEmail" Grid.Row="3" Grid.Column="1"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Số điện thoại:"/>
                    <TextBox x:Name="txtSDT" Grid.Row="4" Grid.Column="1"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Địa chỉ:"/>
                    <TextBox x:Name="txtDiaChi" Grid.Row="5" Grid.Column="1"/>
                </Grid>

                <Button x:Name="btn_update"
                        Content="Cập nhật thông tin"
                        Width="200"
                        Height="45"
                        Margin="0,10,0,0"
                        Background="#1E88E5"
                        Foreground="White"
                        FontWeight="Bold"
                        FontSize="16"
                        BorderThickness="0"
                        Click="btn_update_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>