<UserControl x:Class="PBL3a.UI.Student.StudentScore"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="#F0F2F5" 
             d:DesignWidth="900" d:DesignHeight="600">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Rectangle Width="5" Height="30" Fill="#1565C0" Margin="0,0,15,0"/>
            <TextBlock Text="KẾT QUẢ HỌC TẬP" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#1565C0" 
                       VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Xem theo:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold" Foreground="#555"/>
                <ComboBox x:Name="cboSemester"
                          Width="180"
                          Height="32" 
                          VerticalContentAlignment="Center"
                          SelectionChanged="cboSemester_SelectedIndexChanged">
                    <ComboBoxItem Content="Học kì hiện tại" IsSelected="True"/>
                    <ComboBoxItem Content="Tất cả các kì" />
                </ComboBox>

                <Button x:Name="btnShowData"
                        Content="Lấy dữ liệu"
                        Width="110"
                        Height="32"
                        Margin="20,0,0,0"
                        Background="#1E88E5"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Click="btnShowData_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="-90" RenderingBias="Quality" ShadowDepth="1" Opacity="0.1"/>
            </Border.Effect>

            <DataGrid x:Name="dgvScore"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      Background="White"
                      BorderThickness="1"
                      BorderBrush="#E0E0E0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F0F0F0"
                      RowHeight="40">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#E3F2FD"/>
                        <Setter Property="Foreground" Value="#1565C0"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#BBDEFB"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã Lớp" Binding="{Binding ClassID}" Width="120"/>
                    <DataGridTextColumn Header="Tên Môn Học" Binding="{Binding ClassName}" Width="220"/>

                    <DataGridTextColumn Header="Điểm Số" Binding="{Binding DiemHocTap}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#D32F2F"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Nhận xét của giảng viên" Binding="{Binding NhanXet}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontStyle" Value="Italic"/>
                                <Setter Property="Padding" Value="10,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="3" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnExport"
                    Content="Xuất bảng điểm (.pdf)"
                    Width="180"
                    Height="40"
                    Background="#2E7D32"
                    Foreground="White"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Click="btnExport_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>