<UserControl x:Class="PBL3a.UI.Teacher.NhapDiem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White" Height="500" Width="750">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="76"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- TITLE -->
        <TextBlock Grid.Row="0"
                   Grid.ColumnSpan="6"
                   Text="NHẬP ĐIỂM VÀ NHẬN XÉT"
                   FontSize="26"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <!-- FILTER -->
        <TextBlock Grid.Row="1"
                   Grid.Column="1"
                   Text="Mã lớp học:"
                   VerticalAlignment="Center"/>

        <ComboBox x:Name="comboBox1"
                  Grid.Row="1"
                  Grid.Column="2"
                  IsEditable="True"
                  Height="30"
                  KeyUp="comboBox1_KeyUp"
                  SelectionChanged="comboBox1_SelectedIndexChanged" Margin="0,15,72,15"/>

        <TextBlock Grid.Row="1"
                   Grid.Column="4"
                   Text="Tên lớp:"
                   VerticalAlignment="Center" Height="30" Margin="0,0,41,0"/>

        <TextBox x:Name="textBox1"
                 Grid.Row="1"
                 Grid.Column="4"
                 IsReadOnly="True"
                 Height="30" Margin="84,15,34,15" Grid.ColumnSpan="2" Background="#FFF0EEEE"/>

        <!-- TABLE -->
        <DataGrid x:Name="dataGridView1"
                  Grid.Row="2"
                  Grid.Column="1"
                  Grid.ColumnSpan="5"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Background="White" Margin="0,0,34,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã học sinh" Binding="{Binding AccountID}" Width="200"/>
                <DataGridTextColumn Header="Họ và tên" Binding="{Binding HoTen}" Width="*"/>
                <DataGridTextColumn Header="Điểm" Binding="{Binding Diem}" Width="200"/>
                <DataGridTextColumn Header="Nhận xét" Binding="{Binding NhanXet}" Width="*"/>

            </DataGrid.Columns>
        </DataGrid>

        <!-- BUTTON -->
        <Button x:Name="button2"
                Grid.Row="3"
                Grid.Column="5"
                Content="Lưu điểm"
                Width="120"
                HorizontalAlignment="Left"
                Click="button2_Click" Margin="0,18,0,24"/>
    </Grid>
</UserControl>